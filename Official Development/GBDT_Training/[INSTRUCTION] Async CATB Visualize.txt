0. Make sure you have both tensorflow and tensorboard Python libraries installed in your machine.
1. Open Anaconda Prompt
2. Type in the command `python3 -m tensorboard.main --logdir=>TRAINDIR>` where logdir is the path to the `xxxx_TRAINDIR` folder of CatB Training.